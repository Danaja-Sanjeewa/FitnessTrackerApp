import 'package:flutter/material.dart';

class WorkoutView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Workout')),
      body: Center(child: Text('Workout View')),
    );
  }
}
